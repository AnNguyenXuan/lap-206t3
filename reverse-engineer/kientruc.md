## Công cụ
```
Tools IDA, Ghidra

https://github.com/ReversingID/Awesome-Reversing

Một số từ khóa : 
deobfuscator : giải mã code (chuyển từ mã nguồn bị làm rối sang mã nguồn đọc được)
obfuscator : mã hóa code sang dạng khó đọc
compiler : chuyển đổi mã nguồn thành mã nhị phân
decompiler : chuyển đổi mã nhị phân về mã nguồn
Phần mềm chuyên dụng : de4dot, dnSpy (cho C#)
```
## Tools check bit
```
Imhex : Opensource mã nguồn mở kiểm tra bits ổ cứng chuyên sâu
```

## Một số key
```
còn tôi thì lấy source xong viết lại một tool riêng, không can thiệp game quá sâu, và không debug, hầu như game nào mở lên cũng dùng được, không cần bypass, vì không có debug nên chỉ dùng được những cheat cơ bản như teleport, tốc đánh, tốc chạy ... cho rpg game. còn fps thì dò localplayer và base address vô tư, nên chỉ hack được esp. miễn không có debug thì không thằng anti nào quét cả, kk
```


